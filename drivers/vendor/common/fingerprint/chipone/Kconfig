#
# chipone Fingerprint driver configuration
#

config PLATFORM_FINGERPRINT_CHIPONE
       tristate "generic chipone fingerprint driver"
       default n
       help
       add support for chipone fingerprint driver.
